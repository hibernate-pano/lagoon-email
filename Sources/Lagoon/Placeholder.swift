// Placeholder. Task 8 replaces this with the real SwiftUI app.
import SwiftUI

struct LagoonPlaceholderView: View {
    var body: some View {
        Text("Lagoon — Task 8 fills this")
    }
}

// Top-level code entry (no @main needed when using top-level statements)
LagoonAppPlaceholder.main()

struct LagoonAppPlaceholder: App {
    var body: some Scene {
        WindowGroup("Lagoon") {
            LagoonPlaceholderView()
        }
    }
}
