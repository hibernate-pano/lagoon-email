// Minimal entry point so the executable links. Task 7 replaces this
// with the real Hummingbird app (App.swift) and deletes this file.
@main
struct LagoonServerPlaceholderMain {
    static func main() async {
        print("LagoonServer placeholder — Task 7 wires real app")
    }
}